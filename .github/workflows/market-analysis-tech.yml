name: Tech Market Analysis

on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: '*/10 * * * *' # Every 10 minutes (Tech news is slower than Vahid)
  workflow_dispatch:

jobs:
  analyze-tech:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          pip install -r backend/requirements.txt

      - name: Run Analysis
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
        run: |
          python3 backend/main.py --channels config.txt --output market_data_tech.json --limit 50 --max-duration 300

      - name: Deploy to GH-Pages
        run: |
          # --- DEPLOYMENT TO GH-PAGES ---
          mkdir -p /tmp/deploy_cache
          mv market_data_tech.json /tmp/deploy_cache/market_data_tech.json
          
          if [ -d "media" ]; then
             echo "🧹 Pruning old cache..."
             find media -type f -mtime +1 -delete || true
             mv media /tmp/deploy_cache/media
          fi

          # Switch to gh-pages
          git fetch origin gh-pages || true
          git checkout gh-pages || git checkout --orphan gh-pages
          git reset --hard origin/gh-pages || true

          # Restore
          if [ -f "/tmp/deploy_cache/market_data_tech.json" ]; then
              mv /tmp/deploy_cache/market_data_tech.json ./market_data_tech.json
          fi
          
          if [ -d "/tmp/deploy_cache/media" ]; then
            mkdir -p media
            cp -r /tmp/deploy_cache/media/* media/
          fi
          
          # Commit and Push
          git config --global user.name 'Tech Bot'
          git config --global user.email 'bot@noreply.github.com'
          
          git add market_data_tech.json
          git add media/ || echo "No media folder"
          
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Tech Update $(date)"
            git push origin gh-pages --force
          else
            echo "💤 No changes."
          fi
